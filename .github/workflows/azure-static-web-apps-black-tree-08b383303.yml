name: Azure Static Web Apps CI/CD

on:
  push:
    branches:
      - refactor/dotnet-angular
  pull_request:
    types: [opened, synchronize, reopened, closed]
    branches:
      - refactor/dotnet-angular

jobs:
  build_and_deploy_job:
    if: github.event_name == 'push' || (github.event_name == 'pull_request' && github.event.action != 'closed')
    runs-on: ubuntu-latest
    name: Build and Deploy Job
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true
          lfs: false

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: client/package-lock.json

      - name: Inietta apiBaseUrl di produzione (URL del backend App Service)
        working-directory: client
        run: |
          cat > src/environments/environment.ts <<EOF
          export const environment = {
            production: true,
            apiBaseUrl: '${{ vars.API_BASE_URL || 'https://bennaloz-home-finances.azurewebsites.net' }}',
          };
          EOF

      - name: Install & build Angular
        working-directory: client
        run: |
          npm ci
          npm run build

      # Deploy con il solo deployment token (modalità classica, senza github_id_token):
      # evita la validazione del branch lato OIDC che rifiutava il deploy.
      - name: Build And Deploy
        id: builddeploy
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_BLACK_TREE_08B383303 }}
          action: "upload"
          app_location: "client/dist/client/browser"
          api_location: ""
          output_location: ""
          skip_app_build: true
          skip_api_build: true

  close_pull_request_job:
    if: github.event_name == 'pull_request' && github.event.action == 'closed'
    runs-on: ubuntu-latest
    name: Close Pull Request Job
    steps:
      - name: Close Pull Request
        id: closepullrequest
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_BLACK_TREE_08B383303 }}
          action: "close"
