name: Deploy backend (App Service)

# Pubblica l'API .NET 10 su Azure App Service "bennaloz-home-finances" via OIDC.
# Riusa le federated credentials create da Azure (Deployment Center).
on:
  push:
    branches: [refactor/dotnet-angular]
    paths:
      - 'backend/**'
      - '.github/workflows/azure-backend.yml'
  workflow_dispatch:

env:
  PROJECT: backend/CasaFinanze.Api/CasaFinanze.Api.csproj
  APP_NAME: bennaloz-home-finances

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4

      - name: Set up .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.x'

      - name: Publish
        run: dotnet publish ${{ env.PROJECT }} -c Release -o ${{ github.workspace }}/publish

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: api
          path: ${{ github.workspace }}/publish

  deploy:
    runs-on: ubuntu-latest
    needs: build
    permissions:
      id-token: write   # richiesto per l'OIDC verso Azure
      contents: read
    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: api
          path: publish

      - name: Login to Azure (OIDC)
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_0EC9E7F510AF487FA108BF9C1046B367 }}
          tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_86891C59C99F47B4A27D231686F8CDCF }}
          subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_ADAD7B90F99046F397B056830F3EB9F4 }}

      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v3
        with:
          app-name: ${{ env.APP_NAME }}
          slot-name: 'Production'
          package: publish
